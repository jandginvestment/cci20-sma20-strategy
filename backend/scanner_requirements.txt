# ── Scanner Lambda — heavy deps (no boto3 — pre-installed in Lambda runtime) ───
pandas==2.2.2
numpy==1.26.4
yfinance>=0.2.61
curl_cffi>=0.7
tqdm
greenlet==3.0.3
sqlalchemy>=2.0.0
psycopg2-binary==2.9.9
